USE [master]
GO

-- 互換性レベル 120 (SQL Srever 2014) で実行
ALTER DATABASE [DemoDB] SET COMPATIBILITY_LEVEL = 120
GO

USE DemoDB
DBCC FREEPROCCACHE
GO
SELECT O_CUSTKEY,COUNT(*) FROM ORDERS_CI GROUP BY O_CUSTKEY OPTION (MAXDOP 1)
 


USE [master]
GO
-- 互換性レベル 130 (SQL Srever 2016) で実行
ALTER DATABASE [DemoDB] SET COMPATIBILITY_LEVEL = 130
GO

USE DemoDB
DBCC FREEPROCCACHE
GO
SELECT O_CUSTKEY,COUNT(*) FROM ORDERS_CI GROUP BY O_CUSTKEY OPTION (MAXDOP 1)
 
